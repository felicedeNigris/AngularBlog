div(class="container")
  h1(class="page-header") {{page.title}}
  p {{page.content}}

  div(class="form-horizontal" ng-hide="page.title !== 'Create a Post'" ng-submit="blogPostsController.addPost(newpost)")
    div(class="form-group")
      textarea(class="form-control" rows="3" ng-model="post.text")
      button(type="submit" class="btn btn-default") Publish

//initialize ripple       
script(type="text/javascript").
        $(document).ready(function(){
            $.material.init();
        }); 